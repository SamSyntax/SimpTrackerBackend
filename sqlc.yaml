version: "1"
packages:
  - name: "db"
    path: "./internal/db"
    engine: "postgresql"
    schema:
      - "internal/db/migrations/*.sql"
    queries:
      - "internal/db/queries/*.sql"
    emit_json_tags: true
